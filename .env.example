# Copy this file to .env and list every IP address or DNS name used to open
# HomelabHQ. Keep .env local to the deployment; it is ignored by Git.
# Example: HLHQ_TLS_HOSTS=192.168.1.10,homelabhq.lan
HLHQ_TLS_HOSTS=

# Core (used now)
cryptography>=50.0.1        # credential-at-rest encryption (Fernet + HKDF)
fastapi>=0.141.1,<0.142   # typed ASGI API, OpenAPI, Swagger UI, and ReDoc
uvicorn>=0.52.1,<0.53     # single-worker production ASGI server

# Device transports (curated drivers, Milestone 2+)
paramiko>=3.4            # SSH
pysnmp>=7.1.28             # SNMP
requests>=2.34.2          # HTTP / REST APIs

# Web push notifications (Milestone 4)
pywebpush>=2.4.0          # VAPID web push (pulls py-vapid, http-ece)
